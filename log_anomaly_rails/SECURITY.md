# Security

Report sensitive issues privately to the maintainers. Rotate `RAILS_MASTER_KEY` and registry tokens on any suspected leak. Do not commit `config/master.key` or raw secrets in `.kamal/secrets`.

